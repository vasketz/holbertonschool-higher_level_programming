#!/usr/bin/node
// this script prints all lenguages but using an array and a loop
const langs = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let i = 0;
while (i < langs.length) {
  console.log(langs[i]);
  i++;
}
